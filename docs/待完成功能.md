❌ 未完成 ‌🟢 进行中  ️✔️ 完成

## 待完成功能列表

- 组件属性: 事件
- 组件属性: 设计器和属性 Rect 同步 ✔️
- - 生成 ui 文件 ✔️
- - - 根据 ui 文件生成 Go 功能代码 ✔️
- - - 根据 ui 文件生成 Go 事件代码 ️
- - - 根据 UI 布局文件 恢复到设计器,  打开 xxx.egp 项目文件时 ‌🟢
- - - 预览 运行生成 Go 代码 ✔️
- - - - 运行/构建的消息提示
- 主菜单栏功能
- - 文件
- - - 新建项目 ‌🟢
- - - 新建窗体
- - 编辑
- - 运行
- - - 构建
- - - 清理构建
- - - 运行 ✔️
- - 设置
- - - 构建选项
- - - 环境配置
- - - 项目配置
- - 帮助


## 问题  

- 放置控件: 窗体设计状态时，窗体属性列表未成功隐藏  ️✔️
- 放置控件: 如果调整了宽度情况下, 控件属性列表宽度不正确 ✔️
- 多窗体设计切换: 属性列表未同步切换到当前被切换的窗体正在设计的组件 ✔️
- 多窗体设计: 属性列表加载不正确, 属性列表会重复加载 ✔️
- 属性列表: 属于类型是 class 实例时, 需要给这个属性实例找到对应的已存在的组件类型, 是一个下拉框
- 事件列表: 功能, 是下拉框, 设置事件关联函数
- UI布局文件生成
- - 只针对被修改的属性 ✔️
- - 同步文件属性变更 (当属性变更时, 控件创建时, 控件: x,y,w,h 调整时) ✔️
- 组件右键菜单功能
- - 设计面板 Z 序  ✔️
- - 组件操作 (剪切, 复制, 粘贴, 删除) 
- - 组件树 Z 序同步调整  ✔️
- 组件属性列表: 当某属性变更后, 有关联的属性也要更新, 例如: width, height 和 ClientWidth, ClientHeight
- 属性和Go方法名不一致, 需要把属性列表的属性名修复和Go代码一样. 例如(属性列表: BidiMode > Go: BiDiMode) (BorderStyle > BorderStyleToFormBorderStyle). 解决:从第一个同名的父类获取真实函数名  ✔️
- 组件列表: 集合 Set checkbox 初始状态值未选中 BorderIcons. ✔️ 解决: 在配置文件里覆盖属性值

### linux
- 組建属性列表：宽度  ✔️
- 设计面板：控件拖拽 ✔️ 临时解决: 先区别Linux不隐藏拖拽控制, 后期在解决
- 窗口布局 ✔️

## 其它(发现的问题)
- 布局恢复, 第一次恢复后多窗体时, 连续关闭会导致窗体名称为空""  ✔️ 在生成UI时判断根组件Name()是否为空
- 布局恢复
- - 组件属性是对象 API 设置
- - 非可视化组件的 API 位置设计(只有 Left, Top)  ✔️
- 布局恢复, 新建窗体的名称需要做校验检查
- 